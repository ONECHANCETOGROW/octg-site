For every question I ask in this session, I'll give you an exact structure to follow (field names and, where relevant, a JSON shape).

If you're able to reply with a bare JSON object matching that structure exactly — no explanation, no markdown code fences, no text before or after it — that's the most useful format for me.

If your responses are conversational by design and you can't restrict yourself to raw JSON, that's completely fine. In that case, please give me the same data as a clearly labeled markdown table (or a bullet list for simple values), using the exact field names I give you as the column headers — for example, for a "campaigns" question, a table with columns campaign_name, spend, conversions, clicks, impressions, one row per campaign. Please keep any commentary brief and put it after the table, not mixed into it.

Please confirm which format you'll be using (JSON or a labeled table) so I know what to expect for the rest of this session.
