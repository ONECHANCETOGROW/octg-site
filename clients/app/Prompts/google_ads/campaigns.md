Please analyze the following campaign data.

If you can produce JSON, return ONLY valid JSON with this exact structure:
{
  "campaigns": [
    {
      "campaign_name": "...",
      "spend": 0,
      "conversions": 0,
      "clicks": 0,
      "impressions": 0
    }
  ]
}

If you can't produce raw JSON, that's fine — reply instead with a markdown table, one row per campaign, using exactly these column headers: campaign_name | spend | conversions | clicks | impressions

Data to analyze:
