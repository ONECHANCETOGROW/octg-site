Please provide recommendations based on the account performance.

If you can produce JSON, return ONLY valid JSON with this exact structure:
{
  "recommendations": [
    {
      "what_to_change": "...",
      "why_it_matters": "...",
      "priority": "High|Medium|Low"
    }
  ]
}

If you can't produce raw JSON, that's fine — reply instead with a numbered or bulleted list, one recommendation per item, in this format:
1. [What to change] — [Why it matters] (Priority: High/Medium/Low)

Data to analyze:
